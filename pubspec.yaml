name: app
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  kalil_adt_annotation:
  material_widgets:
  value_listenables:
  vector_drawable:

dev_dependencies:
  build_runner:
  adt_generator:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true